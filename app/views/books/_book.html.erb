	<a href= <%= book_path(book) %> class="thumbnail">
		<%= image_tag "book.svg" %>
		<div class="caption">
			<h3 class='text-center'><%= truncate(book.title) %></h3>
			<h5 class='text-center'><%= truncate(book.author) %></h5>
		</div>
	</a>

<!-- <tr>
	<td><%= link_to truncate(book.title), book %> </td>
	<td><%= truncate(book.author) %> </td>
	<td> <%= book.course.name %></td>  
	<td><%= book.isbn %></td>
</tr> -->
