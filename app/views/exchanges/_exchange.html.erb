<tr>
	<td><%= exchange.id %></td>
	<td> <%= show_type(exchange.listing) %> 
	<%= link_to exchange.listing.book.title, book_listing_path(exchange.listing.book, exchange.listing) %> </td>
	<td> <%= show_price(exchange.listing.min_price, 2) %> </td>
	<td> <%= show_name(exchange.user) %> </td>
	<td> <%= show_name(exchange.listing.user) %> </td>

</tr>
