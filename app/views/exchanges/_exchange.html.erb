<tr>
	<td><%= exchange.id %></td>
	<td> <%= show_type(exchange.listing) %> 
	<%= link_to exchange.listing.book.title, book_listing_path(exchange.listing.book, exchange.listing) %> </td>
	<td> <%= show_price(exchange.listing.min_price, 2) %> </td>
	<td> <%= show_name(exchange.user) %> </td>
	<td> <%= show_name(exchange.listing.user) %> </td>
	<td>
	<% if can? :modify, Exchange %>
  		<%= link_to fa_icon('pencil', text: 'Edit'), edit_exchange_path(exchange), class:'btn btn-xs btn-default'%>
  	<% end %>
  	<% if can? :modify, Exchange %>
		<%= link_to fa_icon('trash-o', text:'Delete'), exchange, method: :delete, data: { confirm: 'Are you sure?'  }, class:'btn btn-xs btn-danger' %>  	
	<% end %>

  	</td>
</tr>
